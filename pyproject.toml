[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "adbtool"
version = "0.0.41"
dependencies = [
  "litefeel-pycommon>=0.4.15",
  "PyYAML",
  "semantic_version",
]
requires-python = ">=3.13"
authors = [
  {name = "litefeel", email = "litefeel@gmail.com"},
]
description = "A friendly android adb command-line tool"
readme = "README.md"
license = {file = "LICENSE"}
keywords = ["Android", "adb"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Topic :: Software Development :: Libraries :: Python Modules",
  "Programming Language :: Python"
]

[project.urls]
Homepage = "https://github.com/litefeel/adbtool"
Repository = "https://github.com/litefeel/adbtool.git"
"Bug Tracker" = "https://github.com/litefeel/adbtool/issues"

[project.scripts]
adbt = "adbtool.adbtool:main"

[dependency-groups]
dev = [
  "pytest",
  "ruff",
]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.format]
line-ending = "auto"
